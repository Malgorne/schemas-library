/**
 * Import { @link module:userSchema | userSchema } from /schemas.
 * Our mongoose's schemas library.
 * In your project, after importing a schema, you have to build the Model.
 * @module schemas-library
*/
import userSchema from './schemas/user';

export default { userSchema };
